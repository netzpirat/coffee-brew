options -= defaults

options += defaults

options /= defaults

options *= defaults

options %= defaults

options ||= defaults

options &&= defaults

options ?= defaults

options <<= defaults

options >>= defaults

options >>>= defaults

options &= defaults

options ^= defaults

options |= defaults

options or= defaults
